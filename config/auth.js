// 
  //    req.ensureAuthenticated = function(req,response,next) {
  //   if(req.isAuthenticated()){
  //      return next();
  //    }
  //    request.flash('error_message','Please log in to to view that resource');
  //    response.redirect('/login');
  //      };
  // req.forwardAuthenticated = function(request,response,next) {
  //    if(!request.isAuthenticated()) {
  //      return next();
  //    }
  //    request.flash('error_message','Log In Failed');
  //    response.redirect('/dashboard');
  //  }
  // };
 